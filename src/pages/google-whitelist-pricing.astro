---
import BaseLayout from '@layouts/BaseLayout.astro';
import LpHero from '@components/sections/lp/LpHero.astro';
import LpWhyPlatform from '@components/sections/lp/LpWhyPlatform.astro';
import LpComparison from '@components/sections/lp/LpComparison.astro';
import LpPhotoVisit from '@components/sections/lp/LpPhotoVisit.astro';
import IndustriesGrid from '@components/sections/IndustriesGrid.astro';
import LpTestimonials from '@components/sections/lp/LpTestimonials.astro';
import PricingSection from '@components/sections/service/PricingSection.astro';
import LpShortForm from '@components/sections/lp/LpShortForm.astro';
import ArrowRight from '@components/icons/ArrowRight.astro';

import { GOOGLE_LP, INDUSTRIES_LP, INDUSTRY_LP_HEADING } from '@data/whitelist-lp';
import { SEWA_PLANS } from '@data/sewa-akun';
import { serviceJsonLd, breadcrumbJsonLd } from '@lib/seo';
import { site } from '@data/site';
import { getLang, useT } from '@i18n/utils';

const lang = getLang(Astro.currentLocale);
const t = useT(lang);
const d = GOOGLE_LP[lang];
const path = lang === 'en' ? '/en/google-whitelist-pricing' : '/google-whitelist-pricing';
const url = `${site.url}${path}`;

const englishPlans: typeof SEWA_PLANS = [
  {
    name: 'Starter', tagline: 'Minimum spending $20',
    price: '5%', unit: 'top-up fee',
    features: ['$20 - $300 per top-up', 'Official Google Whitelist', 'No VAT fees', 'Partner-level appeal support', 'Auto-balance transfer if disabled (T&C apply)', 'Priority support (Standard)'],
  },
  {
    name: 'Growth', tagline: 'Minimum spending $20',
    price: '4.5%', unit: 'top-up fee', featured: true,
    features: ['$300 - $900 per top-up', 'Official Google Whitelist', 'No VAT fees', 'Partner-level appeal support', 'Auto-balance transfer if disabled (T&C apply)', 'Priority support (Priority)'],
  },
  {
    name: 'Scale', tagline: 'Minimum spending $20',
    price: '3.5%', unit: 'top-up fee',
    features: ['Above $900 per top-up', 'Official Google Whitelist', 'No VAT fees', 'Partner-level appeal support', 'Auto-balance transfer if disabled (T&C apply)', 'Priority support (VIP)'],
  },
];

const plans = lang === 'en' ? englishPlans : SEWA_PLANS;

const jsonLd = [
  serviceJsonLd({ name: d.seo.title, description: d.seo.description, url, serviceType: 'Google Ads Whitelist' }),
  breadcrumbJsonLd([
    { name: lang === 'en' ? 'Home' : 'Beranda', url: lang === 'en' ? `${site.url}/en` : site.url },
    { name: lang === 'en' ? 'Account Rental' : 'Sewa Akun', url: `${site.url}${lang === 'en' ? '/en' : ''}/layanan/sewa-akun` },
    { name: 'Google Whitelist Pricing', url },
  ]),
];

const waText = lang === 'en' 
  ? "Hi Tentaklik! I'm interested in the Google Whitelist Account Rental service. Could you share more details?"
  : "Halo Tentaklik! Saya tertarik dengan layanan Sewa Akun Google Whitelist. Bisa minta info lebih lanjut?";
const waUrl = `https://wa.me/6285129992225?text=${encodeURIComponent(waText)}`;
---
<BaseLayout
  title={`${d.seo.title} — Pricing`}
  description={d.seo.description}
  active="layanan"
  jsonLd={jsonLd}
  keywords={d.keywords}
  bareHeader={true}
  bareLayout={true}
  preloadHeroSrc="true"
>
  <LpHero
    eyebrow={d.hero.eyebrow}
    title={d.hero.title}
    titleAccent={d.hero.titleAccent}
    desc={d.hero.desc}
    ctaText={lang === 'en' ? 'See Pricing' : 'Lihat Harga'}
    ctaHref="#pricing"
  />
  
  <LpWhyPlatform 
    platform={d.platform} 
    heading={d.whyHeading} 
    sub={d.whySub} 
    items={d.whyItems} 
  />
  
  <LpComparison platform={d.platform} />
  
  <PricingSection plans={plans} service="Google Ads Whitelist" />
  
  <IndustriesGrid 
    items={INDUSTRIES_LP[lang]} 
    heading={INDUSTRY_LP_HEADING[lang]} 
    bg="warm" 
    center 
    variant="premium"
  />
  
  <LpPhotoVisit 
    heading={d.photoHeading} 
    sub={d.photoSub} 
    photos={d.photos} 
    variant="grid" 
  />
  
  <LpTestimonials />

  <LpShortForm platform="google" />
</BaseLayout>
